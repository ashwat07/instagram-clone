import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    borderRadius: 43,
    borderWidth: 3,
    borderColor: '#aefeef',
    margin: 10,
  },
  image: {
    borderRadius: 40,
    borderWidth: 3,
    borderColor: '#fff',
  },
});

export default styles;
